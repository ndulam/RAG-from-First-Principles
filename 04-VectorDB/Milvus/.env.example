# This folder's Python scripts don't require any API keys or environment
# variables to run - create_milvus_db.py only uses a local embedding model
# (the OpenAIEmbeddingFunction line in that script is commented out).
