# Copy this file to ".env" in this folder and fill in the values you need.
# Every script here loads this file via python-dotenv's load_dotenv().

# OpenAI - used by the SentenceEmbeddingOptimizer-Compression script
OPENAI_API_KEY=sk-your-openai-api-key

# Cohere - used by 01-ContextualCompressionRetriever-Compression.py (follows
# the Cohere SDK's own default env var name)
CO_API_KEY=your-cohere-api-key
